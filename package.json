{
  "name": "acorn",
  "private": true,
  "version": "1.0.0",
  "description": "Acorn — a calm, dyslexia-friendly practice app. The app itself is a single index.html with no build step; this package exists only for the Playwright test harness.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
